import { View, Text, TouchableHighlight } from 'react-native';
import { colors } from '../../constants';
import { styles } from './styles';

const FlightInfo = ({ item, onSelected }) => {
  return (
    <View style={styles.container}>
      
    </View>
  );
};

export default FlightInfo;
